let nombre
let precio
const marcador= {nombre: 'Marcador', precio: '15' };
for (const propiedades in marcador){
    console.log(propiedades + ": " + marcador [propiedades])
}
const lapicera= {nombre: 'Lapicera', precio: '10' };
for (const propiedades in lapicera){
    console.log(propiedades + ": " + lapicera [propiedades])
}
const lapiz= {nombre: 'Lapiz', precio: '5' };
for (const propiedades in lapiz){
    console.log(propiedades + ": " + lapiz [propiedades])
}
const goma= {nombre: 'Goma', precio: '2' };
for (const propiedades in goma){
    console.log(propiedades + ": " + goma [propiedades])
}